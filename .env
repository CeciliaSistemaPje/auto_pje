# Caminho base usado pelo frontend nas requisições.
# Em dev, passa pelo proxy do Vite (ver vite.config.ts) para evitar CORS.
VITE_API_BASE_URL=/api

# Endereço do backend NestJS — alvo do proxy do Vite.
# Altere aqui se o backend rodar em outra porta/host.
VITE_BACKEND_URL=http://localhost:3000
